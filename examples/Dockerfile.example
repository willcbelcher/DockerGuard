# Example Dockerfile with various security issues for testing
FROM ubuntu:latest

# Issue: Using 'latest' tag (BASE_IMAGE_LATEST)

# Issue: Running as root (ROOT_USER)
RUN apt-get update && apt-get install -y curl

# Issue: Hardcoded secret (SECRET)
ENV API_KEY=sk_live_1234567890abcdef
ENV PASSWORD=mysecretpassword123

# Issue: Potential secret in ARG
ARG DB_PASSWORD=changeme

# Copy application
COPY app.py /app/

# Run application as root (no USER instruction)
CMD ["python", "/app/app.py"]

